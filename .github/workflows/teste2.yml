name: testeCI

on:
  push:
    branches: main
    
  workflow_dispatch:  
    
        
jobs:
  build:
    runs-on: ubuntu-latest
  step: 
    - name: Checkout
    - uses: actions/checkout@v3.5.2
    
    - name: Run script
    - uses: echo "deu certo"
  
 
