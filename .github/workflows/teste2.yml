name: testeCI

on:
  push:
    branches: main
    
  workflow_dispatch:  
    
        
jobs:
  name: testeCI 
    build:
     runs-on: ubuntu-latest
    
    steps: 
    - uses: actions/checkout@v3
    
    - name: Run script
    - run: 
        echo "deu certo aqui".
  
 
